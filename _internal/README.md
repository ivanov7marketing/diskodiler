# Internal Project Artifacts

This folder holds non-production project materials.

- `legacy-static/` keeps the archived static site and related prototype assets.
- `workspace/` is for local handoff, audit, planning, and research files.

These files are not part of the Laravel runtime, and the Timeweb deploy workflow now publishes only `laravel/`.
